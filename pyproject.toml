[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
# A sensible starter rule set: pyflakes (F), pycodestyle (E), imports (I).
select = ["E", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
